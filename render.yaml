services:
  - type: web
    name: tfg-ciudades
    env: java
    buildCommand: "./mvnw clean package"
    startCommand: "java -jar target/tfg-ciudades.war"
    envVars:
      - key: PORT
        value: 8080
